package routes

// Full Pages
const (
	IndexPage      = "/"
	MainPage       = "/dash"
	CheckEmailPage = "/checkemail"
)

// Auth
const (
	Verify = "/verify"
	Login  = "/login"
	Logout = "/logout"
	Signup = "/dash"
)

// API + Other
const (
	Static = "/static"
)
