package routes

// Full Pages
const (
	IndexPage      = "/"
	MainPage       = "/dash"
	CheckEmailPage = "/checkemail"
)

// Auth
const (
	VerifyGet  = "/verify"
	LoginPost  = "/login"
	LogoutGet  = "/logout"
	SignupPost = "/dash"
)

// Auth Components
const (
	AuthFormGet = "/authform"
)

// API + Other
const (
	Static = "/static"
)
